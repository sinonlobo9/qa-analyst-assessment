# Remove Duplicates

## Overview

This solution implements a pure function that removes duplicate values from a list while preserving the order of first occurrences.

Example:

```python
remove_duplicates([1, 2, 3, 2, 4, 1, 5])
# Output: [1, 2, 3, 4, 5]